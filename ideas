list swap idea {
    no drag & drop.
    list add button puts current list in storage unless its empty.
    clicking a list in storage will swap lists *ideally animation to indicate this.*
}

decorate scrolls

enter listener for input task


page reload storage duplication. wtf







